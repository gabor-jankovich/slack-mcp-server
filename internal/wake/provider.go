package wake

import (
	"github.com/korotovsky/slack-mcp-server/internal/events/models"
)

// Provider is an alias for the WakeProvider interface in the events package.
type Provider = models.WakeProvider

// Request is an alias for the WakeRequest type.
type Request = models.WakeRequest
